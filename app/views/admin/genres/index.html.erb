<header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a class="navbar-brand p-3" href="/">logo</a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <%= link_to "商品一覧", new_admin_password_path, class: 'nav-link text-dark' %>
            </li>
            <li class="nav-item">
              <%= link_to '会員一覧', new_admin_password_path, class: 'nav-link text-dark' %>
            </li>
            <li class="nav-item">
              <%= link_to '注文履歴一覧', new_admin_password_path, class: 'nav-link text-dark' %>
            </li>
            <li class="nav-item">
              <%= link_to "ジャンル一覧", admin_genres_path, class: 'nav-link text-dark' %>
            </li>
            <li class="nav-item">
              <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: 'nav-link text-dark' %>
            </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <p id="notice"><%= notice %></p>
    <main>
      <div class="container px-5 px-sm-0">
        <div class="row px-5">
          <div class="col-xl-3">
            <h4 class="mt-2">ジャンル一覧・追加</h4>
            <div class="form-inline">
            <%= form_with model:@newgenre, url:admin_genres_path, local:true do |f| %>
              <div class="form-group">
                <%= f.label :name,"ジャンル名", class: "control-label mt-3 mr-4" %>
                <%= f.text_field :name, class: "form-control genre_name mt-3 mr-4" %>
                <%= f.submit "新規登録", class: "btn btn-success mt-3 mr-3" %>
              </div>
            <% end %>
            </div>
            </div>
          </div>
        <div class="row px-5">
          <div class="col-8">
         <table class="table table-sm border-secondary">
          <thead class="table-secondary">
            <tr>
              <th class="col-3 border-secondary text-center">ジャンル</th>
              <th class="col-2 border-secondary"></th>
            </tr>
          </thead>
          <tbody>
            <% @genres.each do |genre| %>
            <tr>
              <td><%= genre.name %></td>
              <td><%= link_to "編集する", edit_admin_genre_path(genre.id), class: "btn btn-success"%></td>
            </tr>
            <% end %>
        　</tbody>
        </table>
    　</div>
    　</div>
    　</div>
   </main>