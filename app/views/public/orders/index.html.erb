<main>
      <div class="container">
        <div class="row">
          <h4 class="col py-4"><span  class="bg-light">注文履歴一覧</span></h4>
        <table class="table table-sm border-secondary">
          <thead class="table-secondary">
            <tr>
              <th class="col-2 border-secondary">注文日</th>
              <th class="col-3 border-secondary">配送先</th>
              <th class="col-2 border-secondary">注文商品</th>
              <th class="col-1 border-secondary">支払金額</th>
              <th class="col-2 border-secondary">ステータス</th>
              <th class="col-2 border-secondary">注文詳細</th>
            </tr>
          </thead>
          <tbody>
            <% @order_details.each do |order_detail| %>
            <tr>
              <td><%= order_detail.order.created_at.strftime("%Y年%m月%d日") %></td>
              <td><%= order_detail.order.address %></td>
              <td><%= order_detail.item.name %></td>
              <td><%= order_detail.order.total_payment %>円</td>
              <td><%= order_detail.order.status_i18n %></td>
              <td><%= link_to "注文履歴", public_order_path(order_detail.id), class: "btn btn-primary" %></td>
            </tr>
            <% end %>
        　</tbody>
        </table>
    　</div>
    </main>